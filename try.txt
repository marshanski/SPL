    if (mSelectionPolicy)
    {
        delete mSelectionPolicy;
    }
    if (coal)
    {
        delete coal;
    }
    if (connections)
    {
        delete connections;
    }
    mAgentId         = other.mAgentId;
    mPartyId         = other.mPartyId;
    mSelectionPolicy = other.mSelectionPolicy;
    coal             = other.coal;
    connections      = new vector<int> *(other.connections.size())
    for (unsigned i = 0; i < other.connections.size(); i++)
    {
        connections.push_back(other.connections.at(i));
    }
    return *this
